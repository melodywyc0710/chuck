# Chuck — Claude Code Instructions

## Git Workflow

After completing any coding task:

1. Commit the changes with a clear message
2. Push to the feature branch
3. **Automatically create a PR and merge it to main** — do not wait for the user to say "merge to main"

Use squash merge. The branch is `claude/serene-heisenberg-wfxdmw` for this session; reset it from `origin/main` at the start of each new task (the branch was merged in the previous session).
